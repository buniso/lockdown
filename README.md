# The Lockdown Index — DH101 Group 4 mini-site (draft skeleton)

A draft GitHub Pages version of the World Happiness Report mini-site, to compare against the Wordspace build.

## What's here
- `index.html` — home page, hero smiley, research questions
- `narrative.html` — full narrative text + placeholders for the timeline, 5+ visualizations, and map
- `bibliography.html` — full annotated bibliography, grouped by team member
- `data-critique.html` — full data critique, grouped by team member's question
- `about.html` — sources/processing/presentation, bios, role table, acknowledgements
- `css/style.css`, `js/main.js` — shared styling + the scroll-driven smiley flip
- `assets/img/` — empty folder, drop illustrations/photos here
- `data/` — empty folder, drop your cleaned CSV(s) here once you're ready to wire up real charts

## Publishing on GitHub Pages
1. Create a new GitHub repo (or use your group's existing one) and push everything in this folder to it.
2. In the repo, go to **Settings → Pages**.
3. Under "Build and deployment," set **Source** to "Deploy from a branch," pick your default branch (e.g. `main`) and folder `/ (root)`.
4. Save — GitHub will give you a URL like `https://yourusername.github.io/repo-name/` within a minute or two.

## Still TODO
- Swap the viz/map/timeline placeholders for real embeds (Tableau Public embed, an `<iframe>` for TimelineJS, a chart library, etc.)
- Fill in the About page's Processing and Presentation write-ups + real bios
- Add credited illustrations/photos to `assets/img/`
- Revisit the visual design pass (smiley/2020 direction) once the skeleton feels right
